1、程序: 由代码和命令组成的静态的存储在设备中
进程: 由使用者执行程序触发，使用者的权限，程序的代码与需要的资料，动态的被加载到内存中
2、Systemd，不能，所有进程的父进程都是systemd，然而systemd与系统服务有关，控制更多的进程并行启动
3、子进程会继承父进程的环境变量和权限
4、进程可以和终端相关，也可以没有关系，和终端相关的进程，可以分为前台运行和后台运行两种，后台运行类似于windows最小化
前台运行的进程
不一定会终止，与用不用nohup有关
5、ps：ps aux 和 ps 的区别：ps只能看到当前bash下的进程信息，ps aux可以看到整个系统下的详细进程信息
top命令相当于windows任务管理器
free -wht：查看内存信息
dmesg：查看进程启动时间
vmstat：查看linux信息
6、进程的权限：由运行进程的用户的权限决定，该进程可以访问那些文件或系统资源
可以

7、在内存当中
/proc/apm 高级电源管理信息
/proc/cmdline 内核命令行
/proc/Cpuinfo 关于Cpu信息
/proc/Devices 可以用到的设备（块设备/字符设备）
/proc/Dma 使用的DMA通道
/proc/Filesystems 支持的文件系统

8、dmesg | grep 'eth'
dmesg命令是将开机的一些硬件启动显示出来

思考题：
windows：单机版软件，或者是网络的个护短；对操作系统理解不需要很深入；
linux：开源主要为服务器软件，对性能，稳定性，可维护性，客观理性，要求较高，
对操作系统的理解需要比较深入，且有丰富的配置文件来对软件在不同场景下面的使用进行调优，
而且需要有丰富的日志记录来便于维护；